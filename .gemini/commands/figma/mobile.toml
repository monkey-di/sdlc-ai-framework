description = "Генерация мобильной версии из выбранного Figma макета"
prompt = """
Я выбрал мобильный макет (Mobile Frame).

Проанализируй его структуру через get_design_context.

Сверстай HTML-структуру (или React компонент), используя только базовые классы Tailwind (без префиксов sm:, md:).

Убедись, что все элементы идут в вертикальном потоке (flex-col), где это необходимо.

Если встретишь сложные элементы (аккордеоны, слайдеры), просто пометь их комментариями `<!-- TODO: Implement accordion -->`.

Выведи только код без объяснений.
"""
