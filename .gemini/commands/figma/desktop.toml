description = "Добавление десктопной адаптивности к ранее сгенерированному коду"
prompt = """
Теперь я выбрал десктопный макет (Desktop Frame) того же блока.

Твоя задача — обновить ранее сгенерированный код, добавив адаптивность.

Алгоритм действий:
1. Сравни структуру десктопа с мобильной версией.
2. Найди контейнеры, которые меняют направление (например, были flex-col, стали flex-row). Добавь к ним класс md:flex-row.
3. Найди элементы, которые скрыты на мобильном, но есть на десктопе. Добавь им hidden md:block.
4. Обнови размеры шрифтов и отступы, используя префикс md: (например, text-sm md:text-lg).

Важно: Не дублируй HTML. Используй одни и те же теги с разными классами.

Если структура DOM радикально отличается (например, Табы вместо Аккордеона), напиши:
<div className="hidden md:block">...Tabs...</div>
и скрой мобильную версию через md:hidden.

Выведи итоговый, объединенный адаптивный код без объяснений.
"""
