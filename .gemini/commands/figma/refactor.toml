description = "Обновление существующего файла под макет Figma (укажите путь через {{args}})"
prompt = """
Я хочу обновить существующий файл {{args}}, чтобы он соответствовал выбранному макету в Figma.

Инструкция:
1. Сначала прочитай текущий код файла с помощью read_file.
2. Затем запроси данные из Figma для текущего выделения (get_design_context + get_variable_defs).
3. Сравни и обнови:
   - Сохрани существующую JS-логику, импорты и обработчики событий (onClick и т.д.). Не удаляй их!
   - Полностью перепиши классы className="..." в HTML/JSX, чтобы они соответствовали отступам, цветам и типографике из Figma.
   - Если в Figma появились новые элементы, аккуратно добавь их в структуру.
   - Если какие-то элементы удалены в дизайне, удали их из кода.

Используй write_file или выведи полный код файла для копирования.
"""
