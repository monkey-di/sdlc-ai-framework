# 🚀 Быстрый старт

## Для Gemini CLI

### Установка команд
```bash
# Команды уже готовы в директории gemini-commands/
# Просто укажите путь при запуске
```

### Типичный сценарий (3 шага)

```bash
# Запустите Gemini CLI в директории проекта:
gemini

# 1. Выделите мобильный фрейм в Figma, затем:
/figma:mobile

# 2. НЕ закрывая диалог, выделите десктопный фрейм в Figma:
/figma:desktop

# 3. Проверка (опционально):
/figma:qa
```

### Обновление существующего файла
```bash
gemini
/figma:refactor путь/к/компоненту.tsx
```

---

## Для Claude Code

### Установка команд
Команды уже установлены в `.claude/commands/` и доступны как слэш-команды.

### Типичный сценарий (2 шага)

```
1. Выделите мобильный фрейм в Figma, затем напишите:
/figma-mobile

2. В том же диалоге выделите десктопный фрейм:
/figma-desktop
```

### Обновление существующего файла
```
/figma-refactor
```
Затем укажите путь к файлу.

### Чистый HTML
```
/figma-html
```

---

## 🎯 Что нужно перед стартом

### Для Gemini CLI:
- ✅ Gemini CLI установлен
- ✅ Настроен Figma MCP Server
- ✅ Есть доступ к Figma файлу

### Для Claude Code:
- ✅ Claude Code установлен
- ✅ MCP сервер Figma настроен в конфигурации
- ✅ Есть доступ к Figma файлу

---

## 📝 Чек-лист перед генерацией

- [ ] Элемент выделен в Figma
- [ ] Auto Layout настроен (HORIZONTAL/VERTICAL)
- [ ] Spacing/Padding заданы (кратны 4px для лучшего результата)
- [ ] Дизайн-токены (Variables) настроены для цветов
- [ ] Шрифты соответствуют Tailwind (или кастомной конфигурации)

---

## 💡 Частые ошибки

| Ошибка | Причина | Решение |
|--------|---------|---------|
| Отступы странные (19px, 13px) | Не настроен Auto Layout | Используйте Auto Layout с spacing кратным 4 |
| Цвета в hex (#3B82F6) | Нет Variables в Figma | Создайте Color Variables или используйте стандартную палитру Tailwind |
| Нет адаптивности | desktop-merge в новом контексте | Запускайте в том же диалоге после mobile-first |
| Фиксированная ширина 1440px | Неправильная интерпретация Frame | Игнорируется автоматически — используется max-w-screen-xl |

---

## 🔥 Продвинутые сценарии

### Генерация всей страницы (секция за секцией)

```bash
# Gemini CLI - генерация всей страницы секция за секцией
# В интерактивном режиме Gemini:
gemini

# Затем для каждой секции:
# 1. Выделите мобильную версию Hero в Figma
/figma:mobile

# 2. Выделите десктопную версию Hero
/figma:desktop

# 3. Скопируйте код, затем повторите для следующей секции
```

### Batch обновление компонентов

```bash
# Claude Code (в диалоге)
for file in src/components/*.tsx; do
  /figma-refactor
  # Укажите $file когда спросит
done
```

---

## 📞 Поддержка

Если команды не работают:
1. Проверьте синтаксис TOML (для Gemini) — нет лишних кавычек
2. Убедитесь, что MCP сервер запущен
3. Проверьте логи: `gemini logs` или Claude Code Developer Tools
4. Прочитайте полный [README.md](./README.md)
