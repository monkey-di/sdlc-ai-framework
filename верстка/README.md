# Готовые команды для автоматизации верстки Figma → Tailwind

Этот набор содержит готовые команды для Gemini CLI и Claude Code, основанные на исследовании методологии автоматизированной генерации интерфейсов.

## 📁 Структура

```
верстка/
├── research.md                    # Исходное исследование
└── README.md                      # Этот файл

.gemini/commands/figma/            # Команды для Gemini CLI (TOML)
├── init.toml                     # Инициализация агента → /figma:init
├── mobile.toml                   # Генерация мобильной версии → /figma:mobile
├── desktop.toml                  # Добавление десктопной адаптивности → /figma:desktop
├── refactor.toml                 # Обновление существующих файлов → /figma:refactor
├── html.toml                     # Генерация чистого HTML → /figma:html
└── qa.toml                       # Визуальная проверка качества → /figma:qa

.claude/commands/                  # Команды для Claude Code (Markdown)
├── figma-mobile.md               # Генерация мобильной версии → /figma-mobile
├── figma-desktop.md              # Добавление десктопной адаптивности → /figma-desktop
├── figma-refactor.md             # Обновление существующих файлов → /figma-refactor
├── figma-html.md                 # Генерация чистого HTML → /figma-html
└── figma-qa.md                   # Визуальная проверка качества → /figma-qa
```

## 🚀 Использование Gemini CLI команд

### Предварительная настройка

1. Убедитесь, что у вас установлен Gemini CLI с поддержкой MCP
2. Настроен Figma MCP Server
3. У вас есть файл конфигурации с доступом к Figma

### Базовый workflow (Dual-Frame)

**Шаг 1: Инициализация (опционально)**
```bash
gemini
/figma:init
```

**Шаг 2: Генерация мобильной версии**
- Выделите мобильный фрейм (375px) в Figma
```bash
gemini
/figma:mobile
```

**Шаг 3: Добавление десктопной версии**
- НЕ закрывайте диалог из шага 2
- Выделите десктопный фрейм (1440px) в Figma
```bash
/figma:desktop
```

**Шаг 4: Проверка качества**
```bash
/figma:qa
```

### Альтернативные сценарии

**Обновление существующего компонента:**
```bash
gemini
/figma:refactor src/components/Header.tsx
```

**Генерация чистого HTML (без React):**
```bash
gemini
/figma:html
```

## 🎯 Использование Claude Code команд

Команды автоматически доступны в Claude Code через слэш-команды:

### Базовый workflow

**Шаг 1: Генерация мобильной версии**
- Выделите мобильный фрейм в Figma
```
/figma-mobile
```

**Шаг 2: Добавление десктопной версии**
- В том же диалоге выделите десктопный фрейм
```
/figma-desktop
```

**Шаг 3: Проверка качества**
```
/figma-qa
```

### Альтернативные сценарии

**Обновление существующего файла:**
```
/figma-refactor
```
Затем укажите путь к файлу, который нужно обновить.

**Генерация чистого HTML:**
```
/figma-html
```

## 📋 Критические правила трансляции

Все команды используют единые правила конверсии:

| Figma → Tailwind | Правило |
|------------------|---------|
| Auto Layout HORIZONTAL | `flex-row` |
| Auto Layout VERTICAL | `flex-col` |
| itemSpacing: 16px | `gap-4` (делим на 4) |
| padding: 24px | `p-6` (делим на 4) |
| width: FILL | `flex-1` или `w-full` |
| Фиксированная ширина 1440px | ❌ Используй `w-full max-w-screen-xl mx-auto` |
| primaryAxisAlignItems: SPACE_BETWEEN | `justify-between` |
| counterAxisAlignItems: CENTER | `items-center` |

## 🎨 Примеры использования

### Пример 1: Новый лендинг

```bash
# Gemini CLI
gemini run gemini-commands/figma-init.toml
# Выделите Hero секцию (mobile)
gemini run gemini-commands/mobile-first.toml
# Выделите Hero секцию (desktop)
gemini run gemini-commands/desktop-merge.toml
```

### Пример 2: Рефакторинг Header

```bash
# Claude Code
/figma-refactor
# Укажите: src/components/Header.tsx
```

### Пример 3: Статический HTML прототип

```bash
# Gemini CLI
gemini run gemini-commands/pure-html.toml > landing.html
```

## ⚠️ Важные замечания

1. **Не очищайте контекст** между mobile-first и desktop-merge — они работают последовательно
2. **Выделяйте элементы в Figma** перед запуском команд
3. **Проверяйте дизайн-токены** через get_variable_defs для корректного маппинга цветов
4. **Используйте семантические теги** в HTML (section, article, nav)
5. **Тестируйте адаптивность** на реальных устройствах, не только в DevTools

## 🔧 Кастомизация

### Изменение стека в Gemini

Отредактируйте `figma-init.toml`:
```toml
[command.prompt]
system = """
...
Стек: Vue + Tailwind CSS  # Или другой стек
...
"""
```

### Изменение breakpoints

В файлах команд замените `md:` на свои:
- `sm:` — 640px
- `md:` — 768px
- `lg:` — 1024px
- `xl:` — 1280px
- `2xl:` — 1536px

## 📚 Дополнительные ресурсы

- [Исследование методологии](./research.md)
- [Figma MCP Documentation](https://github.com/anthropics/model-context-protocol)
- [Tailwind CSS Docs](https://tailwindcss.com)
- [Gemini CLI Guide](https://ai.google.dev/gemini-api/docs/cli)

## 🐛 Troubleshooting

**Проблема**: Команда не находит Figma элемент
- **Решение**: Убедитесь, что элемент выделен в Figma перед запуском

**Проблема**: Неправильные отступы (не кратны 4)
- **Решение**: Проверьте Auto Layout в Figma — Gemini делит px на 4

**Проблема**: Цвета в hex вместо токенов
- **Решение**: Проверьте, что переменные настроены в Figma Variables

**Проблема**: Адаптивность не работает
- **Решение**: Убедитесь, что desktop-merge запущен в том же контексте, что и mobile-first
