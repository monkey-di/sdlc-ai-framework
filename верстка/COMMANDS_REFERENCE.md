# 📖 Справочник команд

## Gemini CLI команды

Все команды доступны после запуска `gemini` в директории проекта.

| Команда | Описание | Использование |
|---------|----------|---------------|
| `/figma:init` | Инициализация агента с правилами трансляции | `/figma:init` |
| `/figma:mobile` | Генерация мобильной версии | Выделите mobile frame → `/figma:mobile` |
| `/figma:desktop` | Добавление десктопной адаптивности | После `/figma:mobile` выделите desktop frame → `/figma:desktop` |
| `/figma:refactor` | Обновление существующего файла | `/figma:refactor src/App.tsx` |
| `/figma:html` | Генерация чистого HTML5 | Выделите элемент → `/figma:html` |
| `/figma:qa` | Визуальная проверка качества | После генерации → `/figma:qa` |

---

## Claude Code команды

Доступны в любом диалоге с Claude Code.

| Команда | Описание | Использование |
|---------|----------|---------------|
| `/figma-mobile` | Генерация мобильной версии | Выделите mobile frame → `/figma-mobile` |
| `/figma-desktop` | Добавление десктопной адаптивности | После `/figma-mobile` выделите desktop frame → `/figma-desktop` |
| `/figma-refactor` | Обновление существующего файла | `/figma-refactor` → укажите путь к файлу |
| `/figma-html` | Генерация чистого HTML5 | Выделите элемент → `/figma-html` |
| `/figma-qa` | Визуальная проверка качества | После генерации → `/figma-qa` |

---

## Типовые сценарии

### 1️⃣ Новый компонент (адаптивный)

**Gemini CLI:**
```bash
gemini
/figma:mobile      # Выделите mobile frame
/figma:desktop     # Выделите desktop frame
/figma:qa          # Проверка (опционально)
```

**Claude Code:**
```
/figma-mobile      # Выделите mobile frame
/figma-desktop     # Выделите desktop frame
/figma-qa          # Проверка (опционально)
```

---

### 2️⃣ Обновление существующего компонента

**Gemini CLI:**
```bash
gemini
/figma:refactor src/components/Header.tsx
```

**Claude Code:**
```
/figma-refactor
```
Затем укажите путь к файлу.

---

### 3️⃣ Статический HTML прототип

**Gemini CLI:**
```bash
gemini
/figma:html
```
Скопируйте вывод в `.html` файл.

**Claude Code:**
```
/figma-html
```

---

### 4️⃣ Только мобильная версия

**Gemini CLI:**
```bash
gemini
/figma:mobile
```

**Claude Code:**
```
/figma-mobile
```

---

## Порядок использования (важно!)

### ✅ Правильно

```mermaid
graph LR
    A[Выделить Mobile Frame] --> B[/figma:mobile]
    B --> C[Выделить Desktop Frame]
    C --> D[/figma:desktop]
    D --> E[Получить адаптивный код]
```

### ❌ Неправильно

```
/figma:mobile
[Закрыть диалог]
[Новый диалог]
/figma:desktop  ← Потеряет контекст!
```

---

## Параметры команд

### /figma:refactor (Gemini)

```bash
# С аргументом (путь к файлу)
/figma:refactor src/components/Button.tsx

# Без аргумента (спросит интерактивно)
/figma:refactor
```

Использует `{{args}}` для подстановки пути к файлу.

---

## Проверка установки

### Gemini CLI

```bash
# Проверьте наличие команд
gemini
/help

# Должны увидеть:
# /figma:init
# /figma:mobile
# /figma:desktop
# /figma:refactor
# /figma:html
# /figma:qa
```

### Claude Code

```
# В любом диалоге введите:
/

# В списке должны быть:
# /figma-mobile
# /figma-desktop
# /figma-refactor
# /figma-html
# /figma-qa
```

---

## Расположение файлов

### Gemini CLI
```
.gemini/commands/figma/
├── init.toml
├── mobile.toml
├── desktop.toml
├── refactor.toml
├── html.toml
└── qa.toml
```

### Claude Code
```
.claude/commands/
├── figma-mobile.md
├── figma-desktop.md
├── figma-refactor.md
├── figma-html.md
└── figma-qa.md
```

---

## Troubleshooting команд

| Проблема | Решение |
|----------|---------|
| Команда не найдена в Gemini | Убедитесь, что файлы `.toml` в `.gemini/commands/figma/` |
| Команда не найдена в Claude | Убедитесь, что файлы `.md` в `.claude/commands/` |
| `{{args}}` не подставляется | Передавайте аргумент: `/figma:refactor путь/к/файлу` |
| Адаптивность не работает | Запускайте `/figma:desktop` в том же диалоге после `/figma:mobile` |
| MCP инструменты не работают | Проверьте конфигурацию MCP сервера Figma |

---

## Кастомизация команд

### Изменить стек технологий

Отредактируйте `.gemini/commands/figma/init.toml`:

```toml
prompt = """
...
Стек: Vue + Tailwind CSS  # Измените на нужный
...
"""
```

### Добавить свои правила

Добавьте в `prompt` любой команды:

```toml
prompt = """
...
5. Дополнительное правило: Используй shadcn/ui компоненты где возможно
...
"""
```

---

## Источники

- [Custom Commands | Gemini CLI](https://geminicli.com/docs/cli/custom-commands/)
- [Gemini CLI: Custom slash commands | Google Cloud Blog](https://cloud.google.com/blog/topics/developers-practitioners/gemini-cli-custom-slash-commands)
- [Claude Code Commands Documentation](https://docs.claude.ai/claude-code)
